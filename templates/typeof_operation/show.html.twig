{% extends 'base.html.twig' %}

{% block title %}TypeofOperation{% endblock %}

{% block body %}
    <h1>TypeofOperation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typeof_operation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ typeof_operation.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('typeof_operation_index') }}">back to list</a>

    <a href="{{ path('typeof_operation_edit', {'id': typeof_operation.id}) }}">edit</a>

    {{ include('typeof_operation/_delete_form.html.twig') }}
{% endblock %}
