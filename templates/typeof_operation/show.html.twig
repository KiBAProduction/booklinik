{% extends 'base.html.twig' %}

{% block title %}Type d'opération{% endblock %}

{% block body %}
    <h1>Type d'opération</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typeof_operation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ typeof_operation.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('typeof_operation_index') }}" class="btn back-btn">Retour</a>

    <a href="{{ path('typeof_operation_edit', {'id': typeof_operation.id}) }}" class="btn edit-btn">Modifier</a>

    {{ include('typeof_operation/_delete_form.html.twig') }}
{% endblock %}
