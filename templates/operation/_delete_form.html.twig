<form method="post" action="{{ path('operation_delete', {'id': operation.id}) }}" onsubmit="return confirm('Êtes vous sur de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ operation.id) }}">
    <button class="btn delete-btn">Supprimer</button>
</form>
