<form method="post" action="{{ path('typeof_operation_delete', {'id': typeof_operation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typeof_operation.id) }}">
    <button class="btn">Delete</button>
</form>
