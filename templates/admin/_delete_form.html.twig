<form method="post" action="{{ path('admin_delete', {'id': admin.id}) }}" onsubmit="return confirm('Êtes vous sur de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ admin.id) }}">
    <button class="btn delete-btn">Supprimer</button>
</form>
